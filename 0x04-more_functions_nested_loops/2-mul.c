#include "main.h"

/**
 * mul - Entry point to multiplication
 *
 * @a: first integral input
 *
 * @b: second integral input
 *
 * Return: Always 0 (Success)
 */
int mul(int a, int b)
{
	int ab = a * b;

	return (ab);
}
