#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int num = 48;
	int bum = 97;

	while (num <= 57)
	{
		putchar(num);
		num++;
	}

	while (bum <= 102)
	{
		putchar(bum);
		bum++;
	}
	return (0);
}
